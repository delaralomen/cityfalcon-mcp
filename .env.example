# Copy to .env (which is gitignored) and fill in your own values.

# CityFalcon (used for both CityFalcon and DCSC endpoints)
CITYFALCON_API_KEY=

# Legacy MarketAux script (financial_mcp.py.legacy)
MARKETAUX_API_TOKEN=
